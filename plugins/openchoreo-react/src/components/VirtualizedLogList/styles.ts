import { makeStyles } from '@material-ui/core/styles';

export const useVirtualizedLogListStyles = makeStyles({
  /** Applied to the virtuoso scroller element. */
  root: {
    width: '100%',
  },
});
