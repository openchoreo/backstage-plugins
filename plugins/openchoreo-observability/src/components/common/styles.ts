import { makeStyles } from '@material-ui/core/styles';

export const useEnvironmentsStatusNoticeStyles = makeStyles(theme => ({
  container: {
    marginTop: theme.spacing(2),
    marginBottom: theme.spacing(2),
  },
}));
