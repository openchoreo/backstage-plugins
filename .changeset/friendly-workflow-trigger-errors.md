---
'@openchoreo/backstage-plugin-openchoreo-ci': patch
---

Surface user-friendly error messages when workflow triggers fail (such as when the referenced Workflow does not exist). Errors are displayed via an alert banner on Build Latest and inline in the Build with Custom Parameters dialog.
